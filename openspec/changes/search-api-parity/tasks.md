## 1. Add Missing Search Parameters

- [ ] 1.1 Add `search_language_filter` and `user_location` to search tool schema — **10 min**
- [ ] 1.2 Wire parameters through handler to `performSingleSearch` — **10 min**
- [ ] 1.3 Update `performSingleSearch` to include new params in request body — **10 min**

## 2. Validation

- [ ] 2.1 Run `npm run build` — **2 min**
