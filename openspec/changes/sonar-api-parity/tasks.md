## 1. Add Missing Parameters

- [ ] 1.1 Add new params to `CommonOptions` interface in `src/types/index.ts` — **5 min**
- [ ] 1.2 Update `buildCommonOptions` in `src/utils/validation.ts` to extract new params — **10 min**
- [ ] 1.3 Update `buildChatBody` in `src/utils/api-client.ts` to include new params — **10 min**
- [ ] 1.4 Add new property definitions to ask, research, reason tool schemas — **15 min**

## 2. Parse search_results Response Field

- [ ] 2.1 Add `search_results` to `PerplexityChatResponse` type — **5 min**
- [ ] 2.2 Update `appendExtras` to format search_results — **10 min**
- [ ] 2.3 Update streaming parser to capture search_results — **5 min**

## 3. Validation

- [ ] 3.1 Run `npm run build` — **2 min**
